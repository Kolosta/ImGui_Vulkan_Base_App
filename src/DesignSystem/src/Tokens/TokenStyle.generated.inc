// AUTO-GENERATED — do not edit by hand.
//
// Regenerated by tools/apply-tokens-json.py from a Token-Graph JSON export.
// Applied at the end of TokenRegistry::CreateDefaultComponentTokens(), so the
// exported style becomes the system's REAL token values (default layer and/or
// per-theme layers), not runtime OverrideManager overrides.
//
// Each entry resolves a token by id and rewrites its default value, a reference,
// or a per-theme context value. Empty by default (identity — no style applied).
//
// Available in scope: `this` is the TokenRegistry; `GetToken(id)` returns the
// shared Token; helpers MakeRef/MakeColor/... below build TokenValues.

// (no generated style — run tools/apply-tokens-json.py --write to populate)
